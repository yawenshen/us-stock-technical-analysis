---
name: us-stock-technical-analysis
description: 融合 Wave+Value 五因子与 Johnny 多周期关键位方法，生成美股盘中趋势确认、收盘复盘、支撑压力、左侧/右侧策略及交易计划。适用于用户请求美股、ETF、半导体或高波动标的的短线技术分析、关键位地图、风险预算、Johnny 风格分析或盘中/收盘确认；仅当用户明确要求纯 Johnny 策略时切换为 Johnny 独立模式。
author: yawenshen
---

# 美股融合技术分析

## 定位

把两套方法组织为同一工作流，而不是机械叠加评分：

- **Wave+Value 层**：快速判断当日趋势、三层价格区间、左侧/右侧/观望和高波动防守模式。
- **Johnny 层**：用多周期结构、离散关键位、Fib/趋势线/EMA/TD/量价共振与风险收益比校验交易计划。
- **纯 Johnny 模式**：仅当用户明确说“Johnny分析策略”“Johnny策略”“用Johnny分析”或“Johnny博主”时启用，不混入 Wave+Value 权重和三层区间。

每次分析使用最新有效数据重算，不沿用前一交易日区间或历史静态点位。

## 数据准备

1. 获取最新复权 OHLCV、当前价、币种、交易所和时区；检查拆股、ADR 比例和单位。
2. 默认准备日线与 4 小时数据；盘中导航可增加 1 小时/15 分钟，长期判断准备足够计算 EMA169 的日线或周线数据。
3. 先检查裸 K 与成交量，再计算 EMA8/13/20/21/50/144/169/200、ATR、标准 TD 九转和所需 Fibonacci。
4. 选择对应基准：半导体优先 SOXX，科技股优先 QQQ/SPX；其他标的选择能解释其风险环境的指数。
5. DRAM 按 Roundhill Memory ETF 识别并提示不是 Dataram；NBIS 单独提示流动性与波动风险。

### Yahoo Finance 403

先区分 **Yahoo 源站 403** 与 **行情连接器/代理层 403**，不得把连接器错误直接判定为 Yahoo 封禁：

1. 默认行情工具返回 403 时，先用当前运行环境中的 `yfinance` Python 包对同一标的、同一时间窗做一次直连校验；历史数据的 `end` 日期按不包含处理。
2. 若直连成功，说明 Yahoo 源站、DNS、TLS 和当前出口可用；本轮改用直连结果，记录为“连接器/代理层异常”，不要创建延迟重试。
3. 若直连也失败，再用带常规浏览器 User-Agent 的 Yahoo Chart API 做一次最小请求，以区分客户端 Cookie/Crumb、频率限制与网络/IP 问题。
4. 只有默认工具、`yfinance` 直连和最小 Chart API 请求均失败，且无法取得可靠行情时，才停止分析；不使用缓存或推测数据。创建 2 小时后的一次性重试，最多重试 3 次并在任务上下文记录轮次；任一次成功即结束重试链。第 3 次仍失败，只记录本轮因连续 403 取消，不补写分析。创建任务前检查同一轮是否已有重试，避免重复。

## 模式路由

### 融合模式（默认）

适用于盘中检查、11 点报告、收盘复盘、短线支撑压力和交易倾向。执行：

1. 用 Wave+Value 五因子判定方向与参考位。
2. 用 Johnny 多周期方法建立内部候选地图并筛选关键位。
3. 把入选点转换为核心交易区、观察带和独立收盘失效线。
4. 输出左侧/右侧/观望，并用 Johnny 的确认、收益风险比和仓位纪律复核。

### 纯 Johnny 模式

仅在明确触发词出现时使用。阅读 [johnny-methodology.md](references/johnny-methodology.md)，输出离散 `S1–S3` / `R1–R3`、市场状态、三套交易计划、持仓方案和风险预算；不强制 Wave+Value 五因子权重、三层 ATR 区间或左侧/右侧标签。

## 融合模式工作流

### 1. Wave+Value 五因子

按以下权重评分：

1. 历史价格结构 35%
2. 均线趋势 20%
3. 成交量/筹码结构 20%
4. Fibonacci 回撤与扩展 15%
5. ATR 波动调整 10%

判断优先级：历史价格结构 ≥ 成交量结构 > 均线趋势 > Fibonacci > ATR。EMA8/13 交叉只增强或削弱均线趋势因子内部评分，不成为第六因子，也不能独立触发交易结论。不要使用“5%量化支撑压力模型”。

### 2. Johnny 关键位校验

内部列出主 Fib、局部导航 Fib、必要时的嵌套锚点 Fib与次级扩展、平台、前高前低、缺口、趋势线、EMA/Vegas、整数位和量价反应候选。

对每个候选检查八项共振：主 Fib、次级扩展、水平结构、趋势线/通道、EMA/Vegas、量价反应、TD/结构信号、基准指数确认。5–8 项为强，3–4 项为中等，1–2 项不得单独成为交易依据。

每侧只保留 2–3 个作用独立的关键位；证据不足可少于 2 个，不用弱点补足。距离近不代表可合并：若周期、结构角色、确认/失效或动作不同，保留为独立点位。动态均线或趋势线写“约/参考位”并注明数据时间。

### 3. 三层执行区间

围绕最终参考位输出：

- 核心交易区：参考位上下 ±0.25 ATR。
- 观察带：参考位上下 ±0.25～0.40 ATR。
- 独立收盘失效线：单列，不与观察带混写。

当 `ATR / price > 8%` 时启用趋势模式：顺趋势区间收窄为 ±0.20 ATR，逆趋势区间扩宽为 ±0.45 ATR。重点检查 COHR、WDC、NBIS、LITE，但以当日计算为准。

### 4. 状态与策略方向

区分盘中触及、盘中站稳和收盘确认：

- **左侧**：价格进入核心支撑区、跌幅充分且没有明显加速下跌，可讨论分批轻仓试错。
- **右侧**：价格从支撑启动，放量突破关键压力并按预设周期收盘确认，可讨论突破回踩。
- **观望**：尚未到支撑、结构持续破坏、处于支撑压力中间或收益风险比不足。

高波动标的在偏弱市场中不得进入积极左侧名单。使用“需收盘确认才考虑”或“趋势模式下仅顺势减仓”等防守措辞；盘中短暂企稳不能升级为主动做多。

### 5. 交易计划与风险

为有效关键位给出条件化计划：

- 激进支撑试仓：4 小时止跌并重新站回关键位后，首笔为计划总仓位的 20%–25%。
- 深度支撑低吸：等待更深 Fib/平台/Vegas 共振及长下影、双底、缩量止跌或放量反包。
- 右侧突破回踩：日线收盘突破压力，回踩不破后入场；EMA8 上穿 EMA13、量价和指数配合提高可信度。

先定失效再算仓位：`可买数量 = 单笔允许亏损金额 ÷ |入场价 - 止损价|`。普通波段风险示例为账户的 0.5%–1%，高波动或杠杆产品为 0.25%–0.5%，优先服从用户上限。第一目标通常分批兑现 25%–50%；左侧计划原则上要求约 2:1，右侧高确认计划可接受约 1.5–2:1。

## 融合模式输出

每个标的依次输出：

1. 一句话结论与数据时间。
2. 当前价、短线偏向、基准指数状态及 `ATR/price`。
3. 关键支撑与压力：每侧 2–3 个，注明来源、结构角色和强度。
4. 核心交易区、观察带、独立收盘失效线。
5. `策略方向：左侧 / 右侧 / 观望（+一句理由）`。
6. 入场确认、失效、目标 1/2、仓位与收益风险比。
7. 数据缺口、财报/流动性/跳空等风险。

标题与正文风险判断保持一致。支撑压力中间区域明确写“不交易/等待”，不为给结论而强行选方向。

## 纪律

- 不预测精确底部，不把单日上涨视为反转。
- 不把 Fib、TD、EMA 交叉或单次触线当作独立交易信号。
- 不复用旧点位；锚点、区间和动态指标均按最新数据重算。
- 技术支撑不能覆盖重大基本面恶化。
- 市值里程碑默认关闭；确有必要时阅读 [johnny-methodology.md](references/johnny-methodology.md)，并坚持周线确认与股数/币种口径披露。
- 所有路径写成条件情景，不写成确定预测。

---
name: us-stock-technical-analysis
description: 融合 Wave+Value 五因子与 Johnny 多周期关键位方法，按“周线定方向 → 日线找结构 → 小时线找时机”生成美股收盘日报、盘中趋势确认、支撑压力、量能背离、左侧/右侧策略及交易计划。适用于用户请求美股、ETF、半导体或高波动标的的短线技术分析、收盘复盘、关键位地图、风险预算、Johnny 风格分析或盘中确认；仅当用户明确要求纯 Johnny 策略时切换为 Johnny 独立模式。
---

# 美股融合技术分析

## 定位

把两套方法组织为同一工作流，而不是机械叠加评分：

- **Wave+Value 层**：快速判断当日趋势、三层价格区间、左侧/右侧/观望和高波动防守模式。
- **Johnny 层**：用多周期结构、离散关键位、Fib/趋势线/EMA/TD/量价共振与风险收益比校验交易计划。
- **纯 Johnny 模式**：仅当用户明确说“Johnny分析策略”“Johnny策略”“用Johnny分析”或“Johnny博主”时启用，不混入 Wave+Value 权重和三层区间。

每次分析使用最新有效数据重算，不沿用前一交易日区间或历史静态点位。

## 数据准备

### 行情获取与数据校验

不限定行情获取工具或传输通道。可使用当前环境中可用的 Yahoo Finance Chart API、`yfinance`、金融行情工具或其他可靠行情源；优先选择能返回最新、完整、可复核 OHLCV 与复权信息的方式。某一通道因 robots、网络、权限、限频或服务异常不可用时，应切换到其他可靠通道，而不是因单一工具失败直接停止分析。

若使用 Yahoo Finance Chart API，可参考：

```text
https://query1.finance.yahoo.com/v8/finance/chart/{SYMBOL}?period1={UNIX_SECONDS}&period2={UNIX_SECONDS}&interval={INTERVAL}&events=div%2Csplits&includeAdjustedClose=true
```

执行规则：

1. 每次记录数据源、获取时间、市场时区、币种和最后一根有效 K 线时间；历史查询的结束时间必须覆盖目标交易日完整交易时段，标的代码需按数据源要求处理。
2. 收盘日报对每个标的必须分别取得周线 `1wk`、日线 `1d`、小时线 `1h`，不得用单一周期代替三周期分析。周线与日线需有足够历史样本以识别完整波段并计算长期 EMA（通常日线至少 2 年、周线至少 3–5 年）；小时线取得近期完整交易时段。盘中任务可按需增加 `15m`；如数据源不提供原生 4 小时周期，可按交易所时区从 `1h` 聚合，但不得替代收盘日报的小时线步骤。
3. 将每个周期整理为时间戳、开盘、最高、最低、收盘、复权收盘和成交量，并检查币种、交易所时区、当前价、拆股、分红、ADR 比例和单位。若数据源缺少某字段，必须披露缺口，不得猜测补值。
4. 三个周期分别按时间戳对齐 OHLCV 并剔除 OHLC 全空行。日线、周线结构优先使用复权收盘价；成交量与小时线结构使用原始 OHLCV，并结合公司行动检查价格口径。
5. 历史报告以目标交易日的最后一根正常交易时段 K 线为截止，不得混入之后交易日。周线若尚未收完，必须标注“本周未收盘”，不把未完成周 K 当作确认信号；小时线只使用正常交易时段并标明最后数据时间。
6. 收盘日报先完成三周期数据完整性检查，再按“周线定方向 → 日线找结构 → 小时线找时机”执行。任一周期缺失时，优先切换可靠数据源补齐；仍无法取得时，单独披露该标的数据缺口，不得用其他周期推测补位。
7. 先检查裸 K 与成交量，再计算 EMA8/13/20/21/50/144/169/200、ATR、标准 TD 九转和所需 Fibonacci。
8. 选择对应基准：半导体优先 SOXX，科技股优先 QQQ/SPX；其他标的选择能解释其风险环境的指数。
9. DRAM 按 Roundhill Memory ETF 识别并提示不是 Dataram；NBIS 单独提示流动性与波动风险。

### 数据异常处理

单一获取通道失败时，记录错误类型并切换其他可靠通道。取得替代数据后，检查时间窗、时区、币种、字段完整性、最新价和公司行动；关键价格若与第二可靠来源明显不一致，先核对复权、盘前盘后、拆股和单位，无法解释则停止对应标的分析并披露冲突。只有当可用通道均无法取得满足要求的数据时才停止；不得使用过期缓存、推测数据或无 OHLCV 明细的网页摘要补位，也不自动创建定时重试任务。

## 模式路由

### 融合模式（默认）

适用于盘中检查、11 点报告、收盘复盘、短线支撑压力和交易倾向。执行：

1. 收盘日报先对每个标的完成周线、日线、1 小时线独立取数与检查，再严格按“周线定方向 → 日线找结构 → 小时线找时机”输出完整分析。
2. 用 Wave+Value 五因子判定方向与参考位。
3. 用 Johnny 多周期方法建立内部候选地图并筛选关键位。
4. 把入选点转换为核心交易区、观察带和独立收盘失效线。
5. 输出左侧/右侧/观望，并用 Johnny 的确认、收益风险比和仓位纪律复核。

### 纯 Johnny 模式

仅在明确触发词出现时使用。阅读 [johnny-methodology.md](references/johnny-methodology.md)，输出离散 `S1–S3` / `R1–R3`、市场状态、三套交易计划、持仓方案和风险预算；不强制 Wave+Value 五因子权重、三层 ATR 区间或左侧/右侧标签。

## 融合模式工作流

### 1. Wave+Value 五因子

按以下权重评分：

1. 历史价格结构 35%
2. 均线趋势 20%
3. 成交量/筹码结构 20%
4. Fibonacci 回撤与扩展 15%
5. ATR 波动调整 10%

判断优先级：历史价格结构 ≥ 成交量结构 > 均线趋势 > Fibonacci > ATR。EMA8/13 交叉只增强或削弱均线趋势因子内部评分，不成为第六因子，也不能独立触发交易结论。不要使用“5%量化支撑压力模型”。

### 2. Johnny 关键位校验

内部列出主 Fib、局部导航 Fib、必要时的嵌套锚点 Fib与次级扩展、平台、前高前低、缺口、趋势线、EMA/Vegas、整数位和量价反应候选。

对每个候选检查八项共振：主 Fib、次级扩展、水平结构、趋势线/通道、EMA/Vegas、量价反应、TD/结构信号、基准指数确认。5–8 项为强，3–4 项为中等，1–2 项不得单独成为交易依据。

每侧只保留 2–3 个作用独立的关键位；证据不足可少于 2 个，不用弱点补足。距离近不代表可合并：若周期、结构角色、确认/失效或动作不同，保留为独立点位。动态均线或趋势线写“约/参考位”并注明数据时间。

### 3. 三层执行区间

围绕最终参考位输出：

- 核心交易区：参考位上下 ±0.25 ATR。
- 观察带：参考位上下 ±0.25～0.40 ATR。
- 独立收盘失效线：单列，不与观察带混写。

当 `ATR / price > 8%` 时启用趋势模式：顺趋势区间收窄为 ±0.20 ATR，逆趋势区间扩宽为 ±0.45 ATR。重点检查 COHR、WDC、NBIS、LITE，但以当日计算为准。

### 4. 状态与策略方向

区分盘中触及、盘中站稳和收盘确认：

- **左侧**：价格进入核心支撑区、跌幅充分且没有明显加速下跌，可讨论分批轻仓试错。
- **右侧**：价格从支撑启动，放量突破关键压力并按预设周期收盘确认，可讨论突破回踩。
- **观望**：尚未到支撑、结构持续破坏、处于支撑压力中间或收益风险比不足。

高波动标的在偏弱市场中不得进入积极左侧名单。使用“需收盘确认才考虑”或“趋势模式下仅顺势减仓”等防守措辞；盘中短暂企稳不能升级为主动做多。

### 5. 收盘日报三步完整分析

收盘日报对股票池中的每个标的逐一执行完整的“周线定方向 → 日线找结构 → 小时线找时机”，不得只对重点标的展开、对其余标的缩写，也不得以汇总表替代三步正文。三推是结构化观察，不是机械数 K 线：从最近一个确认波段低点或高点开始，把同方向推进及其间可辨认回撤依次标为第一推、第二推、第三推；反向有效突破或收盘失效后重新选锚点。

#### 第一步：周线定方向

1. 识别周线当前为上升或下降第几推，并标注初段、中段、末端或验证中；结构不足三推时按实际阶段输出，不强行凑数。
2. 比较当前推动段与前一同方向推动段的价格幅度、收盘位置、推动段均量和关键突破 K 线量能，判断顶部背离、底部背离或无明显背离。不能凭单根缩量 K 线定性。
3. 结合周线平台、前高前低、EMA/Vegas、主 Fib、趋势线和基准指数，给出明确的中期方向结论：偏多、偏空或中性。未完成周 K 只作观察，不作确认。
4. 固定输出：`周线定方向：{上升/下降第几推与阶段}；{量能背离判断}；中期方向：{偏多/偏空/中性}，验证条件：{条件}。`

#### 第二步：日线找结构

1. 独立识别日线当前为上升或下降第几推，并判断是否进入第三推末端。只有接近前高/前低、Fib 扩展、通道边界或关键支撑压力时，才能称为末端。
2. 使用完整推动段均量与关键突破 K 线判断量能背离：价格创新高但量能未同步为顶部背离；价格创新低但下跌推动量能衰减为底部背离；价格与量能同向增强则为无明显背离。
3. 把日线结构放回周线方向中解释：同向为共振，逆向为反弹/回撤或潜在转折验证，不得脱离周线单独下方向结论。
4. 大白话主结论只使用“警惕反转 / 趋势延续 / 验证中”，并说明后续日 K 需要突破、守住或跌破什么结构才能确认。
5. 固定输出：`日线找结构：{上升/下降第几推与阶段}；{量能背离判断}；与周线{共振/背离/尚未确认}。{大白话结论与验证条件。}`

#### 第三步：小时线找时机

1. 仅在周线方向和日线结构确定后寻找近期入场时机；小时线负责执行，不得反过来推翻已确认的大周期方向。
2. 输出 2–3 个关键支撑与 2–3 个上方压力，说明来源、结构角色和强度；再按 ATR 转换为核心交易区、观察带和独立失效线。
3. 明确验证 K 线条件，例如长下影收复、缩量止跌、放量反包、双底颈线突破、突破后回踩不破；不得只写“企稳”或“关注”。
4. 明确触发条件：具体到小时收盘站回/突破哪个价位、成交量相对近期均量的要求，以及基准指数需要配合的状态。条件未满足时写“不交易/等待”。
5. 固定输出：`小时线找时机：关键支撑 {S1/S2/S3}；验证 K 线 {条件}；触发条件 {条件}；上方压力 {R1/R2/R3}；失效 {价位与收盘口径}。`

#### 综合结论

逐标的汇总周线与日线是否共振，给出“偏多 / 偏空 / 观望”之一，列出最关键的入场触发位、失效位、目标压力位和执行建议。背离只表示动能减弱，未出现价格结构确认前不得直接判定反转；周线与日线冲突时默认降级为观望或轻仓验证，不用小时线噪声强行选边。

固定输出：`综合结论：周线/日线{共振或不共振}；当前{偏多/偏空/观望}。关键价位：触发 {x}、失效 {y}、目标 {z}；执行建议：{条件化动作}。`

#### 三推通道图的数据隔离与一致性

收盘日报包含三推通道图时，每个标的必须独立完成“计算 → 数据对象 → 图表文件 → 视觉验证”，禁止先制作一套上涨或下跌模板再替换标题和数值。图中的预测路径是该标的条件情景，不是固定形状的装饰线。

对每个标的执行：

1. **独立计算**：仅使用当前 `symbol` 的 OHLCV、波段锚点、ATR、支撑压力、三推阶段和背离结果，生成该标的专属的 `actualSeries`、`forecastSeries`、`supportLevels`、`resistanceLevels` 和 `invalidationLevel`。切换标的前重建全部派生变量，不能复用上一标的数组、闭包、全局状态、ECharts `option` 对象或缓存结果。
2. **保留价格尺度**：`forecastSeries` 的每个点必须包含日期/序号与绝对价格，图线直接使用这些价格坐标。允许纵轴为可读性自动缩放，但不得把归一化后的统一曲线映射到不同标的；若为了算法计算使用百分比或标准化值，渲染前必须按当前标的基准价反算为绝对价格。
3. **图文同源**：图中预测节点、末端价格、支撑、压力和失效线必须与正文使用同一个当前标的数据对象。不得在正文计算一次、图表另写一套硬编码值。图例与副标题明确标注 `symbol`、数据截止时间、当前三推方向及阶段。
4. **方向校验**：渲染前计算 `forecastSeries[-1].price - forecastSeries[0].price` 的符号，并与大白话结论核对。上升路径终点应高于起点，下降路径终点应低于起点，震荡/验证中应围绕条件区间表达；不一致时停止出图并重新计算，不能只修改文字标签。
5. **逐图验证**：每个标的生成独立 HTMLBox 文件，文件名包含小写 ticker，例如 `assets/three_push_glw.html`。逐个使用 `html_vision.py` 检查：预测线形状与节点数值一致、纵轴覆盖全部价格、支撑压力位置正确、图例 ticker 正确、console 无错误。验证失败的标的只重算并重绘自身，不复用其他标的结果补位。

批量报告增加交叉检查：

- 建立 `{symbol: data_fingerprint}`，指纹至少包含预测起点、终点、方向、节点数量及前三个预测价格；不同标的出现相同指纹时按异常处理并停止交付。
- 若多个标的恰好方向相同，仍要核对各自斜率、回撤节点、绝对价格与关键位；“方向相同”不能导致“路径数组相同”。
- 输出前逐标的断言：HTML 文件名、标题 ticker、数据对象 `symbol`、正文 ticker 四者完全一致；任一不一致即停止交付。

### 6. 交易计划与风险

为有效关键位给出条件化计划：

- 激进支撑试仓：4 小时止跌并重新站回关键位后，首笔为计划总仓位的 20%–25%。
- 深度支撑低吸：等待更深 Fib/平台/Vegas 共振及长下影、双底、缩量止跌或放量反包。
- 右侧突破回踩：日线收盘突破压力，回踩不破后入场；EMA8 上穿 EMA13、量价和指数配合提高可信度。

先定失效再算仓位：`可买数量 = 单笔允许亏损金额 ÷ |入场价 - 止损价|`。普通波段风险示例为账户的 0.5%–1%，高波动或杠杆产品为 0.25%–0.5%，优先服从用户上限。第一目标通常分批兑现 25%–50%；左侧计划原则上要求约 2:1，右侧高确认计划可接受约 1.5–2:1。

## 融合模式输出

每个标的必须依次完整输出，不省略任何一步：

1. 一句话结论，以及周线、日线、1 小时线各自的数据截止时间。
2. 当前价、基准指数状态及 `ATR/price`。
3. `第一步｜周线定方向`：当前第几推与阶段、上升/下降、量能背离、中期方向和验证条件。
4. `第二步｜日线找结构`：当前第几推、是否末端、量能背离、与周线是否共振及日线确认条件。
5. `第三步｜小时线找时机`：关键支撑、验证 K 线、具体触发条件、上方压力和小时收盘失效条件。
6. 核心交易区、观察带、独立收盘失效线。
7. `综合结论`：周线/日线共振状态、偏多/偏空/观望、关键触发位、失效位、目标位和执行建议。
8. `策略方向：左侧 / 右侧 / 观望（+一句理由）`，并给出仓位、收益风险比及目标 1/2。
9. 数据缺口、未完成周 K、财报/流动性/跳空等风险。

标题与正文风险判断保持一致。支撑压力中间区域明确写“不交易/等待”，不为给结论而强行选方向。

## 纪律

- 不预测精确底部，不把单日上涨视为反转。
- 不把 Fib、TD、EMA 交叉或单次触线当作独立交易信号。
- 不复用旧点位；锚点、区间和动态指标均按最新数据重算。
- 技术支撑不能覆盖重大基本面恶化。
- 市值里程碑默认关闭；确有必要时阅读 [johnny-methodology.md](references/johnny-methodology.md)，并坚持周线确认与股数/币种口径披露。
- 所有路径写成条件情景，不写成确定预测。
